# Cluster-tools HTTPRoutes (ArgoCD, Grafana, Headlamp) are now managed
# by the cluster-infra chart. Each route lives in its own namespace
# and uses the shared-gateway label to access the gateway.
# See: cluster-infra/templates/cluster-tools-httproutes.yaml
