# Prod gateway — set your real domain here.
# DNS A record must point rentlora.in → your EC2 public IP.
hostname: "rentlora.in"


# When adding Grafana/ArgoCD later, they create their own HTTPRoute
# in their namespace pointing to this prod gateway with:
#   parentRefs: [{name: rentlora-gateway, namespace: prod}]
#   hostnames: ["grafana.rentlora.com"]
# No changes needed to this chart.
