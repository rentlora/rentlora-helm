apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceName }}
  annotations:
    # IRSA role from environments/<env>/values.yaml, keyed by service name.
    eks.amazonaws.com/role-arn: {{ index .Values.irsaRoleArns .Values.serviceName | quote }}
