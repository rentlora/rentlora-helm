# ── Prod overrides ────────────────────────────────────────────────────────────
# image.tag is set dynamically by CI/CD: v<semver>  e.g. v1.2.0
image:
  tag: "sha-eeee9f7"

replicaCount: 2

config:
  mongoHost: "mongodb.rentlora-prod.svc.cluster.local"
  mongoDbName: "rentlora_prod"
  logLevel: "INFO"

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
